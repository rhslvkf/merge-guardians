/**
 * The post-wave 3-card draft.
 *
 * Draws 3 distinct cards from upgrades.json after filtering out anything
 * already capped or whose condition fails (spec section 8). Opening the panel
 * is a gameplay interruption, so it pairs with `gameplayStop()`.
 *
 * Stub — implemented in Phase 4.
 */

export class UpgradePanel {
  // TODO(phase-4)
}

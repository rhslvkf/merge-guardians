/**
 * Shared button: pointer states, disabled state with a reason label, and a hit
 * area never smaller than MIN_TOUCH_CELL_PX.
 *
 * Stub — implemented in Phase 3.
 */

export class Button {
  // TODO(phase-3)
}

/**
 * Top HUD: lives, energy bar, gold, wave/stage, boss health bar, pause.
 *
 * On wide layouts the extra side space carries the wave detail and the list of
 * upgrades taken this run.
 *
 * Lives in UIScene. Reads RunState, redraws on `GameEvent` messages (rule 7).
 *
 * Stub — implemented in Phase 3.
 */

export class Hud {
  // TODO(phase-3)
}
